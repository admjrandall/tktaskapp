{
  "name": "@tktaskapp/server",
  "private": true,
  "version": "0.1.0",
  "description": "Task App CRM enterprise server — Hono v4, Drizzle ORM, PostgreSQL, OPA, OTel",
  "type": "module",
  "engines": {
    "node": ">=22.20.0",
    "pnpm": ">=11"
  },
  "dependencies": {
    "hono": "^4",
    "@hono/node-server": "^1",
    "drizzle-orm": "^0.45.2",
    "pg": "^8",
    "@azure/identity": "^4",
    "@azure/keyvault-keys": "^4",
    "@opentelemetry/sdk-node": "^0.218.0",
    "@opentelemetry/auto-instrumentations-node": "^0.76.0",
    "@opentelemetry/exporter-trace-otlp-http": "^0.218.0",
    "@opentelemetry/api": "^1",
    "@open-policy-agent/opa-wasm": "^1",
    "jose": "^6.2.3",
    "valibot": "^1.4.0",
    "ioredis": "^5",
    "@aws-sdk/client-kms": "^3"
  },
  "devDependencies": {
    "@types/pg": "^8",
    "esbuild": "^0.28.0",
    "tsx": "^4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit && esbuild src/index.ts --bundle --platform=node --target=node22 --outfile=dist/server.js",
    "start": "node dist/server.js",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "db:validate": "node ../scripts/validate-migrations.mjs",
    "db:migrate": "tsx src/db/migrate.ts",
    "db:seed": "tsx src/db/seed.ts",
    "test": "vitest run --passWithNoTests"
  }
}
