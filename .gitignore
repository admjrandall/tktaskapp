node_modules/
**/node_modules/
dist/
**/dist/
*.local
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
.turbo/
**/.turbo/
.vscode/

# Tool caches
.eslintcache
*.tsbuildinfo

# Scratch / session artifacts
lint-output.txt
*lint-output.txt
upgrade-session-prompt.md

# axe report — uploaded as CI artifact, not stored in repo
axe-report.json

# Microsoft Office temporary lock files
~$*

# Credential / token files — never commit
ghp_*.txt
*.token
*_token.txt

# Claude Code local settings (machine-specific permission allowlist)
.claude/settings.local.json

# BMAD installer-managed per-user config
_bmad/config.user.toml

# Local agent/tooling installs and Codex desktop state
.agents/
.codex/
