// Shared Vitest settings imported by the root vitest.config.ts and individual
// project configs. Coverage lives in the root config only (per Vitest docs).
export const sharedConfig = {
  globals: true,
  hookTimeout: 30_000,
}
